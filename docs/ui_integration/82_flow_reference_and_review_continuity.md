# 이미지 검토 연속성과 Flow 무제출 준비 검증

검증일: 2026-07-17 KST

## 한 줄 결론

이미지 결과를 먼저 검토하고 필요한 항목만 다시 만드는 작업대 흐름은 실제 Electron에서 저장·재실행까지 통과했다. Flow는 마지막 장면의 참조 0장 무제출 준비는 실제 작업창에서 통과했지만, 중간 장면의 참조 2장 경로는 두 번째 참조 연결 시간 초과로 아직 부분 통과다.

## 이번 변경

- 이미지 결과가 연결돼도 `사용` 또는 `다시 만들기`를 고르지 않았다면 영상 작업보다 결과 검토를 먼저 안내한다.
- 생성 준비 화면의 접힌 자료 구역을 열지 않아도 `결과 검토로` 버튼이 보인다.
- Flow 영상 참조는 장면 순서대로 구성한다.
  - 마지막 전 장면까지: 현재 장면 이미지 + 다음 장면 이미지, 정확히 2장
  - 마지막 장면: 참조 0장
  - Grok과 Replicate: 기존처럼 현재 장면 이미지 1장
- 일부 장면만 다시 만들 때도 재제작 목록의 다음 항목이 아니라 전체 스토리보드의 실제 다음 장면을 참조한다.
- Flow 실행 미리보기는 Electron main에서만 절대 경로와 CDP 연결을 조합한다. renderer에는 경로, CDP 주소, 내부 차단 코드가 노출되지 않는다.
- Flow 명령은 `shell=false`, `preview_only=true`, `live_submit_allowed=false`, `--no-submit`으로 고정한다.

## 실제 Electron 검증

증거 루트:

`/Users/jessiek/.codex/visualizations/2026/07/17/flow-review-continuity-20260717T051030KST`

실제 작업대에서 다음 순서를 확인했다.

1. 이미지 3/3, 영상 1/1이 연결된 상태에서 `다음 할 일: 결과 검토`와 상단 `결과 검토` 버튼이 표시됐다.
2. 장면 이미지를 `다시 만들기`로 고르자 `다음 할 일: 이미지 1개 다시 만들기 준비`로 바뀌고 영상 결과는 오래된 결과로 사용되지 않았다.
3. 같은 이미지를 다시 `사용`으로 바꾸자 영상 검토가 이어졌다.
4. 영상까지 `사용`으로 고르자 `확인 완료 · 선택한 결과를 사용할 수 있습니다.`와 사용 4건이 표시됐다.
5. 앱을 종료하고 같은 user-data로 다시 실행하자 검토 4건과 `클립을 검토하고 사용할 구간을 선택하세요` 단계가 복원됐다.

화면 증거:

- `review-complete.jpeg`
- `relaunch-restored.jpeg`

## 실제 Flow 무제출 검증

### 참조 0장 · 마지막 장면

판정: **PASS**

- 실제 Chrome CDP의 기존 Flow 프로젝트 탭 사용
- 영상 설정 `9:16`, 1개, `Omni Flash` 적용 및 검증
- 프롬프트 입력 후 생성 버튼 활성 상태 확인
- 최종 상태: `READY_NOT_SUBMITTED_VIDEO_TEXT`
- 제출과 과금 생성: 실행하지 않음

증거: `flow-text-pass/`

### 참조 2장 · 중간 장면

판정: **PARTIAL / 실제 작업창 차단**

- 영상 설정 적용: PASS
- 이미지 2개 업로드 요청: PASS
- 첫 번째 참조를 프롬프트에 연결: PASS
- 두 번째 참조를 프롬프트에 연결: `TimeoutError`
- 제출과 과금 생성: 실행하지 않음

현재 앱의 `[2, 0]` 계획·경로·명령 계약은 자동 테스트로 통과했지만, 현재 `google_labs_flow_auto` 작업창 자동화가 두 번째 참조를 끝까지 연결하지 못하므로 실제 Flow 전체 영상 파이프라인은 아직 완료로 판정하지 않는다.

증거: `flow-refs-timeout/`

## 자동 검증

- 전체 Node 테스트: `428/428 PASS`
- Flow/DST/Replicate 실행 provider 파일 테스트: `33/33 PASS`
- UI 집중 테스트: `72/72 PASS`
- `npm run lint`: PASS
- `npm run build`: PASS, 78 modules
- `git diff --check`: PASS
- 독립 최종 코드 검토: P0/P1/P2 없음

## 사실 구분

| 범위 | 판정 | 의미 |
|---|---|---|
| 작업대 이미지 검토·재제작·영상 전환 | PASS | 실제 Electron 조작과 저장 복원 확인 |
| Flow 참조 `[2, 0]` 로컬 실행 계약 | PASS | 정확한 순서·개수·무제출 명령 검증 |
| Flow 참조 0장 실제 작업창 준비 | PASS | 프롬프트와 활성 생성 버튼까지, 미제출 |
| Flow 참조 2장 실제 작업창 준비 | PARTIAL | 두 번째 참조 연결 시간 초과 |
| Flow 유료 생성 결과 | 미실행 | 프로젝트 규칙에 따라 제출하지 않음 |
| 생성물 품질과 사람 승인 | 미검토 | 실제 생성물을 만들지 않았으므로 별도 |
